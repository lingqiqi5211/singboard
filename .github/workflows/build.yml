name: Build

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing release tag to build (for example, v3.3.1)
        required: true
        type: string
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    name: Build Windows app
    runs-on: windows-latest
    env:
      SINGBOARD_PANEL_UPDATE_REPO: ${{ github.repository }}
      WINDOWS_CERT_PFX_B64: ${{ secrets.WINDOWS_CERT_PFX_B64 }}
      WINDOWS_CERT_PASSWORD: ${{ secrets.WINDOWS_CERT_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
          fetch-depth: 0
          fetch-tags: true

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Install pnpm
        run: npm install --global pnpm@11.13.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: actions/cache@v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Read release tag
        shell: pwsh
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_TAG: ${{ inputs.tag }}
          PUSH_TAG: ${{ github.ref_name }}
        run: |
          $tag = if ($env:EVENT_NAME -eq "workflow_dispatch") { $env:INPUT_TAG } else { $env:PUSH_TAG }
          if ($tag -notmatch '^v\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?(?:\+[0-9A-Za-z.-]+)?$') {
            throw "Invalid release tag: $tag"
          }

          $head = git rev-parse HEAD
          $tagCommit = git rev-list -n 1 "refs/tags/$tag"
          if ($LASTEXITCODE -ne 0 -or $tagCommit -ne $head) {
            throw "Release tag $tag does not point to the checked-out commit"
          }

          "RELEASE_TAG=$tag" >> $env:GITHUB_ENV
          "APP_VERSION=$($tag -replace '^v', '')" >> $env:GITHUB_ENV

      - name: Validate release version
        shell: pwsh
        run: |
          $metadata = cargo metadata --locked --no-deps --format-version 1 | ConvertFrom-Json
          $package = @($metadata.packages | Where-Object { $_.name -eq "singboard" })
          if ($package.Count -ne 1) {
            throw "Expected exactly one singboard package in Cargo metadata"
          }

          $packageVersion = $package[0].version
          if ($packageVersion -ne $env:APP_VERSION) {
            throw "Release tag $env:RELEASE_TAG does not match package version $packageVersion"
          }

      - name: Import code signing certificate
        if: env.WINDOWS_CERT_PFX_B64 != ''
        shell: pwsh
        run: |
          $pfxPath = Join-Path $env:RUNNER_TEMP "code-sign.pfx"
          [IO.File]::WriteAllBytes($pfxPath, [Convert]::FromBase64String($env:WINDOWS_CERT_PFX_B64))
          $password = ConvertTo-SecureString $env:WINDOWS_CERT_PASSWORD -AsPlainText -Force
          $cert = Import-PfxCertificate -FilePath $pfxPath -CertStoreLocation Cert:\CurrentUser\My -Password $password
          Remove-Item $pfxPath

          # Trust the self-signed cert on this ephemeral runner so that
          # "signtool verify /pa" can validate the certificate chain.
          $rootStore = [System.Security.Cryptography.X509Certificates.X509Store]::new("Root", "LocalMachine")
          $rootStore.Open("ReadWrite")
          $rootStore.Add($cert)
          $rootStore.Close()

          $signtool = Get-ChildItem "${env:ProgramFiles(x86)}\Windows Kits\10\bin" -Recurse -Filter signtool.exe |
            Where-Object { $_.FullName -match '\\x64\\' } |
            Sort-Object FullName -Descending |
            Select-Object -First 1
          if (-not $signtool) { throw "signtool.exe not found in Windows SDK" }

          "SIGNTOOL=$($signtool.FullName)" >> $env:GITHUB_ENV
          "CERT_THUMBPRINT=$($cert.Thumbprint)" >> $env:GITHUB_ENV

      - name: Build and sign service host
        if: env.CERT_THUMBPRINT != ''
        shell: pwsh
        working-directory: src-tauri
        run: |
          cargo build --release -p singboard-service
          & $env:SIGNTOOL sign /sha1 $env:CERT_THUMBPRINT /fd SHA256 /td SHA256 /tr http://timestamp.digicert.com target/release/singboard-service.exe
          if ($LASTEXITCODE -ne 0) { throw "signing singboard-service.exe failed" }

      - name: Build Tauri app
        run: pnpm tauri build

      - name: Sign application
        if: env.CERT_THUMBPRINT != ''
        shell: pwsh
        run: |
          # The service host was signed before being embedded; fail loudly if
          # the Tauri build relinked it and wiped the signature.
          & $env:SIGNTOOL verify /pa src-tauri/target/release/singboard-service.exe
          if ($LASTEXITCODE -ne 0) { throw "singboard-service.exe lost its signature during the Tauri build" }

          $files = @(Get-Item src-tauri/target/release/singboard.exe)
          $files += Get-ChildItem src-tauri/target/release/bundle -Recurse -File -Include *.exe, *.msi -ErrorAction SilentlyContinue
          foreach ($f in $files) {
            & $env:SIGNTOOL sign /sha1 $env:CERT_THUMBPRINT /fd SHA256 /td SHA256 /tr http://timestamp.digicert.com $f.FullName
            if ($LASTEXITCODE -ne 0) { throw "signing $($f.Name) failed" }
            & $env:SIGNTOOL verify /pa $f.FullName
            if ($LASTEXITCODE -ne 0) { throw "signature verification failed for $($f.Name)" }
          }

      - name: Set build time
        shell: pwsh
        run: |
          $tz = [System.TimeZoneInfo]::FindSystemTimeZoneById("Taipei Standard Time")
          $now = [System.TimeZoneInfo]::ConvertTimeFromUtc([DateTime]::UtcNow, $tz)
          "DATE=$($now.ToString('yyyy-MM-dd HH:mm:ss')) UTC+8" >> $env:GITHUB_ENV

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3.0.2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: ${{ env.APP_VERSION }}
          body: |
            Build time: ${{ env.DATE }}
          files: |
            src-tauri/target/release/singboard.exe
